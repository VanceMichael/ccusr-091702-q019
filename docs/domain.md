# 领域约定

水域赛事安全指挥核围绕公开水域计时、安全冻结、救援和成绩封存保存业务事实。主要对象为赛事版本、选手、计时设备、检查点事件、救援资源、赛段、异常案、成绩版本。

必须长期遵守以下约束：计时事件不能越过人工安全状态；赛段冻结联动救援覆盖；改道需要职责分离；成绩更正发布新版本。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。